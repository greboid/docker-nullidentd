**This has been archived.**  
See [identd](https://github.com/greboid/identd) for a lightweight alternative.

Docker container for nullidentd.  Returns "ident" as the response to all queries.

docker run -d -p 113:113 --name nullidentd greboid/nullidentd
